# M3 E2E 증거 — 스테이징 배포 URL 실관측 (2026-07-17)

> 배포: `https://upstage-sixsense-staging.vercel.app/test.html` (사용자 Vercel, REST API 배포 — `scripts/deploy-staging.mjs`)
> 방법: Playwright 실브라우저 구동. 스크린샷: `assets/2026-07-17-m3-e2e.png`
> 스테이징 = 이 레포 API + 무스타일 검증 페이지. 본 UI 통합은 hanipmap 이식 단계(범위 밖).

## E2E 3종 결과 (plan step-4 Verify)

| # | 흐름 | 관측 결과 | 판정 |
|---|---|---|---|
| ① | 사진 업로드 → `/api/parse-menu` → 프리필 | mujesushi.jpg 업로드 → **후보 12개**가 수정 가능한 폼으로 프리필, "스시C→스시 C" 수정 후 제출 → `status=pending` insert 페이로드 생성(수정 반영 확인). 자동 저장 없음. | PASS |
| ② | 자연어 검색 → `/api/parse-query` | "8천원 이하 도보 5분 혼밥" → `{budget:8000, walkMax:5, tags:["혼밥"], source:"solar"}` — 배포 환경에서 Solar 실경로 동작 | PASS |
| ③ | 학식 크롤러 산출 → 추천 풀 노출 | fixture(패컬티 중식 4일, 9000원) 페이지 렌더 | PASS |

## Failure probe (plan step-4)

- **키 미설정 배포**에서 `/api/parse-menu` → HTTP 500 `{"error":"server misconfigured","code":"no_api_key"}` — 키 노출·크래시 없음 확인 후 env 등록·재배포. PASS
- 콘솔 에러는 favicon.ico 404 1건뿐(무해).

## 배포 기록

- 프로젝트 `upstage-sixsense-staging` 신규 생성(REST). **Vercel Deployment Protection(SSO)이 기본 활성이라 401** — `ssoProtection: null` PATCH 로 공개 전환 (데모 접근용, 신규 공개 *리포* 아님·코드 비공개 유지).
- env: `UPSTAGE_API_KEY` (production/preview, encrypted). 값 비노출.
- 재배포 재현: `node scripts/deploy-staging.mjs [--set-env]` (VERCEL_TOKEN 필요).

## hanipmap 이식 시 남는 것 (팀원 권한 도착 후)

1. `api/parse-menu.js`·`api/parse-query.js`·`api/_lib/` 복사 + hanipmap Vercel 에 `UPSTAGE_API_KEY` env 등록.
2. 제보 폼(사진 업로드→프리필)을 기존 폼에 연결 — test.html §① 로직이 레퍼런스.
3. `db/schema.sql`(cafeteria_menus) 적용 + 크롤러 산출 upsert + 검색 풀에 학식 병합.
4. TRD 반영: Solar 모델=solar-pro2·타임아웃 2.5s (changeset 20260717-parse-query-api 실측 근거).
