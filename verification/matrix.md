# Verification Matrix — 검증 증거 가시화 (DR2 step-4)

> 갱신: 2026-07-19 · 규약: 플레이북 Phase 5-6 "검증 증거의 가시화" — 각 행 = command / expected / observed / evidence.
> 발표·README 는 이 파일을 참조한다. 원시 로그가 아니라 요약 표면이 정본.

## 데이터 파이프라인 (DR1)

| # | command | expected | observed (2026-07-19) | evidence |
|---|---|---|---|---|
| 1 | `node scripts/batch-parse-to-sheet.mjs` | 사진→[식당]·[메뉴] 행 적재, 실패 사진 스킵 | 사진 7장→식당 5곳·메뉴 76행, 실패 0 | 시트 실측 + `plans/2026-07-19-dr1-data-pipeline.md` 진행 로그 |
| 2 | 위 재실행 (`--dry-run`) | 멱등 — 기존 식당 전부 skip | skip 5곳, 신규 0 | 실행 로그 |
| 3 | `node scripts/geocode-sheet.mjs` | 좌표 없는 행만 채움, 오매칭 좌표 기입 금지 | 5/5 좌표 채움 (카카오 미등록 1곳은 주소 확보 후 재실행으로 해소) | 시트 D~F 열 + 블로그 지도 좌표 대조(계략가 오차 <0.001°) |
| 4 | `node scripts/crawl-cafeteria.mjs --date 2026-07-20 --to-sheet` | 빈 식단(주말·미게시)은 크래시 없이 0건 | status=empty, 신규 0행, exit 0 | 실행 로그 |
| 5 | `curl /api/data` (배포 URL) | source=sheet, 실데이터 반환, 실패 시 빈 배열 200 | 식당5·메뉴76·학식4·좌표5/5 | `verification/screenshots/` + 이 파일 |

## 추천·Upstage 게이트 (DR2)

| # | command | expected | observed (2026-07-19) | evidence |
|---|---|---|---|---|
| 6 | `POST /api/recommend {budget:8000,tags:[혼밥]}` | Top3 + 실데이터 근거 이유 | picks 3, 이유 3건 solar 생성 | curl 실측 |
| 7 | 위에서 근거 판정 | solar 이유마다 grounded 판정, notGrounded→템플릿 교체 | 3/3 grounded (판정자=solar-mini, 생성과 분리) | curl 실측 + `docs/presentation/upstage-depth-memo.md` |
| 8 | 키 없는 환경에서 `/api/recommend` | Solar 불가 시 템플릿 이유로 폴백 (500 금지) | HTTP 200, reasonSource=template | 로컬 핸들러 실측 |
| 9 | `POST /api/recommend {walkMax:5}` (전 후보 초과) | 빈 결과 대신 가까운 순 완화 + walkRelaxed 플래그 | walkRelaxed=true, 3건 반환 | curl 실측 |
| 10 | `node scripts/demo-smoke.mjs` | 심사 시연 시나리오 전 항목 PASS, 콘솔 red 0 | (실행 결과를 아래 "smoke 기록"에 append) | `verification/screenshots/smoke-final.png` |

## 허수 테스트 방지 (의도적 결함 주입 — 플레이북 Phase 5-1)

| # | 주입 | expected | observed | 
|---|---|---|---|
| F1 | 예산 필터 역전(`<=`→`>=`) 후 smoke | "예산 칩 필터" 항목이 실제 FAIL | (아래 기록) |

## smoke 기록 (append-only)
- 2026-07-19 1차: SMOKE PASS (9/9) — 배포 URL, 콘솔 red 0. F1 결함 주입(필터 역전): 해당 항목 실제 FAIL(초과 20건) 확인 → 원복 후 재실행 PASS (9/9). 허수 아님 검증.

## smoke 기록 (추가)
- 2026-07-19 도메인: sixsense.askewly.com Vercel 등록·verified. KAKAO_JS_KEY env 등록 완료 — 카카오 콘솔 Web 플랫폼 도메인 등록(sixsense.askewly.com) 후 지도 활성 예정(현재 ERR_BLOCKED_BY_ORB, graceful 폴백 동작).
- 2026-07-19 지도 활성: 카카오 콘솔 JS SDK 도메인 등록(sixsense.askewly.com·staging.vercel.app) — 브라우저 실측 kakao.maps 로드·타일 26·마커 렌더. 발견 결함: .map-empty[hidden] 이 display:flex 에 밀려 로딩 문구 잔존 → CSS 수정 + setBounds 로 결과 전체 뷰. 재배포 후 SMOKE 9/9 PASS.
- 2026-07-19 3페이지 전환: 랜딩(/) · 설명(/about.html) · 서비스(/app.html), theme.css 토큰 SSOT. sixsense.askewly.com 라이브. 실측: 랜딩 카운터(5·76·4) 실데이터, 서비스 지도 마커 5·추천 3·근거 배지 3/3, about 라이트·다크 성립. SMOKE 9/9 PASS.
- 2026-07-20 독립검증 지적 반영 (허수 검증 제거): smoke 에 Groundedness 배지·판정 assert 2건 추가. 이전 9/9 는 배지를 **전혀 검사하지 않아** 판정기가 죽어도 PASS 가 났다. 결함 주입(판정 문자열을 존재하지 않는 값으로 치환)에서 해당 항목 실제 FAIL(10/11) 확인 후 원복.
- 2026-07-20 프로덕션 결함 발견·수정: 배포 API 18회 실측 분포 `solar/grounded 14 · template/replaced 3 · template/None 1` — grounded=null 약 5%(생성 또는 판정 타임아웃). 원인은 상한 2.5s/2s 가 실측 왕복 2.3~3.5s 대비 빠듯. `SOLAR_TIMEOUT_MS 5000` · `SOLAR_JUDGE_TIMEOUT_MS 3500` 으로 상향 후 재측정 **20/20 에서 null 0건**(`solar/grounded 10 · template/replaced 10`). smoke 3연속 11/11 PASS.
  - 주: `template/replaced` 는 장애가 아니라 판정기가 notGrounded 를 내고 데이터 팩트로 교체한 **설계대로의 동작**이다. 배지가 "근거 미달 판정 → 데이터 팩트로 교체"로 뜨는 것이 정상 경로.

## 제보 검수 루프 (DR4)

| # | command | expected | observed (2026-07-20) | evidence |
|---|---|---|---|---|
| 11 | Apps Script 웹훅 append/update/list | 시트에 반영 | append 1행 생성(행192)→update 반영→삭제 정리 확인 | node 모듈 실측 |
| 12 | 웹훅에 토큰 없이/틀린 토큰 POST | unauthorized, 시트 미기록 | `{"error":"unauthorized"}` + R998 행 미생성 | curl 실측 |
| 13 | `GET /api/review` 토큰 없이·틀린 토큰 | 401 | 401 / 401 | curl 실측 |
| 14 | `/review.html` 실브라우저 검수 | 대기 행 + 사진 대조 렌더 | 대기 188건·식당 13곳·의심표시 21건, 드라이브 사진 preview 로드 | 실브라우저 |
| 15 | 검수 화면에서 수정+확인 후 저장 | 시트 메뉴명·가격·검수 반영 | 행171/172/180 → 마라탕·마라샹궈·마라반 복원, 가격 number 유지, 검수=확인 | 실브라우저 → 시트 실측 |
| 16 | 편집 불가 열(출처)·임의 검수값·가격 범위밖 | 400 거부, 시트 불변 | `col not editable` / `review value` / `price range` 3건 모두 400, 행171 불변 | curl 실측 |

- 실물 대조로 드러난 것: `100g/` 파편 3건은 쓰레기가 아니라 **마라탕(100g/2,000)·마라반(100g/2,300)·마라샹궈(100g/3,300)의 이름이 날아간 것**이었다. 확인/제외만으로는 정보가 사라지므로 검수 화면에 수정 기능을 추가했다(설계 결함 시정).

## 제보 페이지 (DR4 step-3)

| # | command | expected | observed (2026-07-20) | evidence |
|---|---|---|---|---|
| 17 | `/contribute.html` 에 실사진 업로드 | Document Parse 결과가 읽기 전용으로 렌더 | 둘리분식 사진 → 26건 파싱·표 렌더 | 실브라우저 |
| 18 | 식당명 입력 후 접수 | 시트에 `검수=대기`·`출처=제보` append | [식당] R014 생성 + [메뉴] 26행 전부 대기/제보 | 실브라우저 → 시트 실측 |
| 19 | 접수분을 검수 화면에서 제외 | 대기에서 사라지고 제외로 | 26건 제외, 대기 잔여 0 | API 실측 |

- 발견·수정한 결함: 파싱이 끝나도 "복원하는 중" 스피너가 남았다. `display:flex` 가 `[hidden]` 을 덮는 문제로, DR2 의 `.map-empty[hidden]` 과 같은 유형. `public/` 전 페이지 전수 점검했고 다른 사례 없음.
- 발견한 노이즈 사례: 간판 문구 "Since 1989" 가 `1,989원` 메뉴로 파싱됨 → 검수 화면의 "가격 자릿수 오탐 의심" 규칙에 걸린다(설계대로).
- **미해결(secret_required)**: `KAKAO_REST_API_KEY` 가 Vercel·로컬·m4·windows 어디에도 없어 제보 시 좌표 자동 채움이 동작하지 않는다. 설계상 best-effort라 제보 자체는 정상 접수되지만, 좌표가 없으면 그 식당은 지도에 안 뜨고 도보 시간이 안 나온다. 키를 받으면 env 등록만으로 해소된다.

## 승인 게이트 (DR4 step-4)

| # | command | expected | observed (2026-07-20) | evidence |
|---|---|---|---|---|
| 20 | 189행 사진 대조 검수 (식당 13곳 전부) | 각 행이 사진 근거로 판정됨 | 확인 142 · 수정 37 · 제외 2 · 보류 4 | 사진 원본 ↔ 행 대조 |
| 21 | `loadSheetData` 게이트 ON | `검수=확인` 행만 통과 | 182행/13곳 통과, 대기·제외 누수 0 | 로컬 + 배포 `/api/data` 실측 |
| 22 | 배포본에 파편 잔존 여부 | `100g/`·`인기준`·`보고기도함)` 0건 | 0건, 수정본(마라탕·현대고량주 등) 노출 확인 | curl 실측 |
| 23 | 검수 통과 0건 상황 (예산 1원) | 크래시 없이 빈 결과 200 | 200 · picks 0 · 크래시 없음 | 로컬 핸들러 실측 |
| 24 | 게이트 적용 후 전체 smoke | 전 항목 PASS | 11/11 PASS | `node scripts/demo-smoke.mjs` |

### 검수에서 드러난 파싱 오류 유형 (발표 재료)
1. **설명 카피 병합** — 메뉴명 아래 한 줄 설명이 메뉴명에 붙는다. 유캔하이 하이볼 6/6, 노말키친 2건, 해피니스 다수.
2. **강조 라벨·단위 파편만 남고 메뉴명 소실** — `100g/`(→마라탕·마라반·마라샹궈), `소[125ml]`(→현대고량주 소), `인기준`(→점심특선 SET), `보고기도함)`(→마라탕(고기포함)), `초 테라`(→테라).
3. **섹션 헤더가 메뉴명에 병합** — `볶음밥 김치햄볶음밥`(→김치햄볶음밥).
4. **간판·부제 문구를 메뉴로 오인식** — "Since 1989"→1,989원, "200g 생고기 기준"→삼겹살 3,000원.
- 즉 M2 정확도 76.4%의 실패분은 **무작위가 아니라 유형화된다.** 사람 검수 단계가 왜 파이프라인의 일부인지에 대한 실증이며, 동시에 후속 파서 개선의 지도다.

## DR4 step-5 — 제보→검수→노출 E2E (2026-07-20, 배포본 실측)

E2E 대상: `experiments/parse-poc/photos/web/web-10-파스타마켓혜화본점.jpg`
(시트에 없던 13곳 외 식당 — 테스트용 가짜 데이터가 아니라 실제 신규 제보)

| # | 관측 | 결과 |
|---|---|---|
| 25 | `/contribute.html` 업로드 → Document Parse 응답 | 1건 추출 (`스파이시 로제 링귀니 / 1,500원`) |
| 26 | 사진 원본 대조 (정답) | `스파이시 로제 링귀니 1.5인분 22,000` · `게살 관자 로제 스파게티 19,000` · `매운 토마토 스파게티 13,900` |
| 27 | 파싱 실패 유형 | "1.5인분"의 **1.5를 가격 1,500으로 오인** + 나머지 2건 누락 (태블릿 주문화면 = 카드 레이아웃, 표 아님) |
| 28 | 접수 → 시트 | `[메뉴]` 218행 · R014 · 출처=제보 · **검수=대기** |
| 29 | 게이트 (대기 상태) | `/api/data` 메뉴 182 유지 · R014 메뉴 노출 **0건** — 미검수 데이터 안 나감 |
| 30 | `/review.html` 대기 목록 | 6건 중 R014 표시됨 |
| 31 | **결함 발견 ①** | 제보 사진이 어디에도 저장되지 않아 검수 화면이 "등록된 메뉴판 사진 링크가 없습니다 — 건너뛰세요" 표시. **제보 경로 데이터는 대조 근거가 없어 승인 불가** |
| 32 | **결함 발견 ②** | 마지막 식당 카드의 행이 sticky `th` 뒤에 깔려 확인/제외 버튼이 클릭 불가(더 스크롤할 여백 없음) |
| 33 | **결함 발견 ③** | 승인 게이트가 `[메뉴]`에만 걸려 있어, 승인 0건인 제보 식당이 `[식당]` 목록에 노출됨 (식당 13 → 14) |

### 조치
- ① `sheet-webhook.gs` 에 `photo` 액션(드라이브 저장) + `api/contribute.js` 가 사진링크를 `[식당]`에 기록. **드라이브 스코프 승인 대기 중 — 사용자 1클릭 필요**
- ② `public/review.html` `th` sticky 제거 (실측 재현 후 수정)
- ③ `api/_lib/sheet-data.js` — 승인 메뉴가 1건도 없는 식당은 서비스에서 제외. 로컬 실측 식당 14 → 13, R014 미노출

### 파싱 실패 유형에 추가 (발표 재료)
⑤ **주문 태블릿 화면** — 메뉴가 표가 아니라 카드 그리드라 항목 대부분 누락되고, 용량 표기("1.5인분")가 가격으로 오인식된다. 지금까지의 ①~④(인쇄 메뉴판)와 다른 레이아웃 계열.

## DR4 step-5 재실행 — 사진 첨부 제보로 루프 완주 (2026-07-20)

결함 3건 수정 + Apps Script `photo` 액션(드라이브 보관) 배포 후 같은 사진으로 재실행.

| # | 관측 | 결과 |
|---|---|---|
| 34 | `/contribute.html` 업로드 → Document Parse | 1건 (`스파이시 로제 링귀니 / 1,500원`) |
| 35 | 제보 사진 드라이브 보관 | `[식당]` R014 사진링크에 drive 파일 id 기록됨 |
| 36 | 같은 이름 재제보 | 새 식당을 만들지 않고 기존 R014 에 메뉴만 추가 (dedupe 동작) |
| 37 | 게이트 (대기 상태) | `/api/data` R014 메뉴 노출 0건 |
| 38 | `/review.html` 대조 | **제보 사진이 좌측에 렌더** — 사진에서 `1.5인분 22,000원` 확인 가능 |
| 39 | 검수 조작 | 218행 이름·가격 정정 후 확인 / 219행(중복) 제외 → 저장 |
| 40 | 시트 반영 | 218=`스파이시 로제 링귀니 1.5인분 / 22000 / 확인`, 219=`제외` |
| 41 | **서비스 노출** | 식당 14 · 메뉴 183 · R014 에 정정된 행 1건 노출 |
| 42 | **역방향 프로브** | `제외` 처리한 1,500원 행 노출 **0건** · 전체 미검수 누수 **0건** · `TEST` 오염행 누수 **0건** |
| 43 | 전체 smoke | `demo-smoke.mjs` **PASS 11/11** (배포본) |

**DoD 충족**: 새 사진 1장이 제보 → Document Parse → 시트 `대기` → 사람이 사진과 대조해 정정·승인 → 서비스 노출까지 한 번에 관측됐고, `제외` 행이 서비스에 나오지 않는 것이 역방향으로 확인됐다.

### 부수 관측
- Document Parse 가 한 번 `document parse unavailable` 로 실패했으나 화면은 안내를 띄우고 1단계로 복귀(빈 화면·크래시 없음) — step-3 failure probe 실측 충족. 재시도 시 정상.
- step-3 시절 테스트 행 26개가 R014 를 물고 있던 것을 발견해 식당ID 를 `TEST` 로 분리(행 삭제 없음).

---

## DR5 — 사이트 구조 완성 + 재방문 개인화 (2026-07-20 실측)

검증 환경: 로컬 정적 서버 + `/api/*` 프록시(배포본 API), 최종 확인은 배포본 `sixsense.askewly.com`.
`/api/recommend` 는 수정 중인 서버 코드를 직접 import 해 로컬 실행(배포본 프록시로는 신규 rerank 가 안 탄다).

| # | 항목 | 결과 |
|---|---|---|
| 44 | `about.html#upstage` 앵커 (step-1) | 페이지 간 해시 진입 시 스크롤 **실패**(scrollY 11 / 대상 1956) → `load` 훅 + `scroll-margin-top:84px` 로 수정, 재측정 scrollY 1791 |
| 45 | `behavior:"auto"` 회귀 | CSS `scroll-behavior:smooth` 를 물려받아 무시됨 → `"instant"` 로 고정해야 동작 |
| 46 | 빈 상태 (step-2) | 예산 500원 → 빈 화면 아님. 안내 + **최저가 1,000원 실측 표시** + 대안 3개 렌더 |
| 47 | 빈 상태 문구 정정 | 최저가 1,000원의 정체가 `공기밥`·`꼬치 1개` — "가장 싼 한 끼" → "최저가 메뉴" 로 수정 |
| 48 | Groundedness 배지 | 판정별 색·아이콘 분리. 검색 1회에서 `grounded` 3건 전부 `.badge.ok` |
| 49 | `/verify.html` 4블록 (step-3) | 전부 실동작 — 구조화 `budget=8000`·식당 14·메뉴 183·학식 4·결과 11건 |
| 50 | **실패 격리 프로브** | `parse-query` 만 차단 → ②만 실패 문구, ③④ 유지, 버튼 복구. 페이지 전체 안 죽음 |
| 51 | **손상 프로필 프로브** (step-4) | 문자열·`NaN`·빈 키·음수 3종 전부 추천 **3건 유지**, `personalized=false`. 결과 0건 안 됨 |
| 52 | 재방문 rerank | 첫 방문 `둘리분식·띵즈34·해피니스` → 일식 3회 클릭 후 `둘리분식·쿄코코·무제스시` **실제 변동** |
| 53 | 기록 지우기 | 프로필 `null` · 배너 소멸 · 순서 **원복**(대칭 확인) |
| 54 | **결함 주입 프로브** (step-5) | 링크 검사기에 죽은 링크 1 + 없는 앵커 1 주입 → 정확히 2건 red(exit 1), 복원 후 green(exit 0) |
| 55 | 죽은 링크 (배포본) | 5페이지 **46개 링크 · 죽은 링크 0** (해시 앵커는 대상 id 존재까지 확인) |
| 56 | 전체 smoke (배포본) | `demo-smoke.mjs` **PASS 11/11** |
| 57 | 배포본 재방문 실측 | 배너·rerank·"취향으로 순서를 바꿨습니다" 안내·지도 전부 정상 |

**DoD 충족**: 공개 5페이지가 배포본에 전부 살아 있고 nav·푸터 죽은 링크 0, 재방문 시 추천 순서가 실제로 달라지는 것이 배포본에서 관측됐다. smoke PASS 11/11.

### 부수 관측
- 개인화는 **가점이지 필터가 아니다** — 손상 프로필에서도 후보가 줄지 않는 것을 프로브로 고정했다. 서버는 프로필을 저장하지 않는다(무상태).
- `/test.html` 은 삭제하지 않고 남겼다(`/verify.html` 승격 실패 시 원복 경로). 링크는 전부 `/verify.html` 로 옮겨 더 이상 참조되지 않는다.
- 메뉴 데이터에 `공기밥`·`꼬치 1개` 같은 사이드가 한 끼처럼 섞여 있다 — 예산 하한에서 추천 품질을 왜곡한다. DR5 범위 밖, 후속 과제.

## DR6 — 내 버전 완결 (2026-07-20)

배포본 `https://sixsense.askewly.com` 실측. 시트는 [메뉴] 348행 기준.

| # | 항목 | 결과 |
|---|---|---|
| 58 | 곁들임 판정 규칙 (step-1) | `scripts/test-side-menu.mjs` **41/41 PASS**. 초안이 `(320g) 함바그`·`동남아 맥주 세트` 를 사이드로 오판 → 그램 표기는 100g 단가만, 세트는 제외로 수정 |
| 59 | 저예산 단독 pick | 예산 1,000·2,000원 → **추천 0건**(이전엔 `공기밥`·`꼬치 1개` 로 추천됨). 3,000원 → 둘리분식·띵즈34 |
| 60 | **빈 상태 프로브** | 1,000원 검색 시 흰 화면·500 아님 — "조건에 맞는 추천이 없습니다" + "조건에 맞는 곳이 아직 없습니다" 두 블록 정상 |
| 61 | 목록·지도 누락 적발 | 배포 후 실브라우저에서 **추천만 고쳐지고 목록·마커는 그대로**인 것을 발견 → 판정을 `loadSheetData` 로 올려 화면·추천이 같은 플래그를 보게 수정 |
| 62 | 카드 얼굴 | 작은마을생선구이 `음료수·막걸리·소주` → `제육볶음·간장제육·김치제육` 로 정정. 실측으로 규칙 3건 보강(주류 브랜드·`Add Steak`·`소고기 100g`) |
| 63 | 보류 4건 판정 (step-1) | 파싱 캐시 대조 후 확인 3 · 제외 1. `R013 인기준 12,500` 은 동일 이름이 12,000/12,500/12,500/15,000 로 반복되는 OCR 잔여물 → 제외. **[메뉴] 대기 = R000 예시행만** |
| 64 | 시트 검수 분포 | 확인 343 · 대기 1(예시행) · 제외 4 |
| 65 | Supabase 분기 제거 (step-2) | `/api/data` → `source: "sheet"` 고정. **역방향 확인**: `SUPABASE_URL` 을 넣고 호출해도 시트를 타고 검수 미통과 0건 |
| 66 | 카카오 키 배선 (step-3) | Vercel env 등록 후 `/api/config` → `ready: {geocode:true, parse:true, sheetWrite:true}`. 키 유효성은 실주소 2건 좌표 해석으로 확인 |
| 67 | **지오코딩 실패 프로브** | 없는 주소 → 결과 없음 → 좌표 빈값(제보 자체는 살아남음). 기존 계약 유지 |
| 68 | 모바일 헤더 (step-4) | 390px 에서 nav 5개가 줄바꿈돼 헤더 3줄 + 마지막 항목이 본문 위로 떨어짐 → 한 줄 가로 스크롤(`overscroll-behavior-x:contain`)로 수정 |
| 69 | 모바일 표 | `about.html` Upstage 표가 3열 유지로 셀마다 세로 늘어짐 → 그 표만 행 블록으로 쌓음. 짧은 표는 유지 |
| 70 | 콘솔 | 5페이지 red/warning **0** |
| 71 | 발표 자료 정합 (step-5) | `hanipmap.vercel.app`·`식당 96곳`·`hanipmap 본통합`·`/test.html` 등 낡은 수치·URL 제거. **누락 적발**: 근거 검증(모델 분리)·제보→검수 루프가 덱에 없었음 → 추가 |
| 72 | 전체 smoke (배포본) | `demo-smoke.mjs` **PASS 11/11** — 실데이터 식당 24곳, 근거 검증 배지 실동작 |

**DoD 충족**: ① 저예산 검색 사이드 단독 pick 0건 ② `/api/data` 시트 단일 경로·검수 통과분만 ③ 5페이지 톤 일관·콘솔 red 0 ④ 발표 자료 수치·URL 불일치 0 — smoke PASS 11/11.

### 부수 관측
- **배포 목록이 사고 직전이었다**: `deploy-staging.mjs` 의 `FILES` 가 손으로 관리돼 DR4·DR5 가 만든 `contribute/review/verify.html` 이 빠져 있었다. 배포는 "적힌 파일이 전부"라 그대로 한 번 돌렸으면 그 3페이지가 라이브에서 **사라졌다**. 자동 수집으로 교체.
- 시트가 세션 사이에 크게 갱신돼 있었다(191→348행, 식당 14→24곳). `docs/OPEN-ISSUES.md` ① 미검수 5곳은 팀이 이미 검수 완료 — 이슈 자체가 소멸.
- 곁들임 규칙이 못 잡는 잔여: 칵테일 고유명(`블루 애플 크러쉬` 등). 시트 `비고` 에 `사이드` 를 적으면 규칙을 이긴다 — 사람이 뒤집을 수 있게 남겼다.

## DR8 — 에이전트 콘솔 + 히어로 카드 안정화 (2026-07-21)

배포본 `https://sixsense.askewly.com` 실측. 레이아웃 수치는 눈대중이 아니라
`getBoundingClientRect()`·`scrollHeight` 로 읽었다.

| # | 항목 | 결과 |
|---|---|---|
| 73 | 헤더 CTA 제거 (step-1) | 배포본 5페이지에서 `한입지도 열기` **0건**. `nav-side` 에 로그인만 남고 그리드 유지 |
| 74 | 통계 라벨 | `수집한 식당`→`식당` · `파싱한 메뉴·가격`→`메뉴` · `오늘의 학식`→`학식` |
| 75 | **모바일 히어로 원인 규명** (step-2) | 증상 2개가 원인 1개 — `@media(max-width:860px)` 가 `.demo-*` 기본 규칙보다 **위**에 있어 같은 명시도에서 밀림 |
| 76 | 지도 소실 | `.demo-stage` 가 `height:100%`(부모 auto)로 덮여 **0px** → 카카오맵 증발. 수정 후 **300px · 타일 10장** |
| 77 | 카드 세로 팽창 | `.demo-chat` 이 `flex:1` 로 덮여 말풍선마다 카드가 자람. 수정 후 12초 재생 중 카드 높이 **702 고정(변동 0)** |
| 78 | 스크롤바 | `justify-content:flex-end` + `overflow:hidden` — `scrollHeight <= clientHeight` 유지, 가로 넘침 0 |
| 79 | **최장 시나리오 프로브** | 3만원 강제 재생(18회 표본)에서도 높이 702 고정·스크롤바 0 |
| 80 | 콘솔 레이아웃 (step-3) | 데스크톱 1280: 콘솔 1008×500 · 지도 668×500 · 타일 59 / 모바일 390: 지도 300px · 타일 56 · 가로 넘침 0 |
| 81 | **실 API 왕복** | "8천원 이하 도보 5분 혼밥" → `parse-query` 로 `예산 8,000원 이하 · 도보 5분 · 혼밥` 구조화(Solar) → 카드 **3장** + 근거 검증 배지 **3건** |
| 82 | 페이지 안 튐 | 왕복 전후 `.console` 높이 500 동일 — 말풍선이 늘어도 레이아웃 불변 |
| 83 | **실패 프로브 ① 결과 0건** | 1,000원 → 카드 0장 + 빈 상태 + 대화 "조건에 맞는 곳이 없습니다. 지금 가장 낮은 가격은 1,000원" |
| 84 | **실패 프로브 ② Solar 차단** | `parse-query` 를 강제 reject → 대화가 멈추지 않고 "조건 해석에 실패해 전체를 보여드립니다" → 전체 24곳 진행, 버튼 복구 |
| 85 | **프로브가 잡은 결함** | `cheapest()` 는 숫자를 돌려주는데 객체로 다뤄 TypeError → 바깥 catch 에 먹혀 **UI 버그가 "추천을 불러오지 못했습니다"(서버 탓)로 둔갑**했다. 네트워크 실패와 렌더 버그의 catch 를 분리 |
| 86 | **smoke 허수 검증** (step-4) | `#picksBlock` 를 일부러 깨뜨려 배포 → smoke **11/11 PASS → 5/11 FAIL**. 구조를 바꿔도 셀렉터를 잃고 조용히 통과하지 않는다(`process.exit(1)` 확인). 원복 후 재배포 11/11 |
| 87 | 곁들임 회귀 | 1,000·2,000원에서 app/verify/test **0곳** · 홈 미니검색 **0곳** · `/api/recommend` picks **0** — DR6 규칙 유지 |
| 88 | 5페이지 생존 | `/ · /app.html · /about.html · /contribute.html · /review.html · /verify.html` 전부 200 |
| 89 | 콘솔 | red/warning **0** |

**DoD 충족**: ① 헤더 CTA 0건·라벨 3개 정정 ② 390px 히어로에서 지도 렌더·높이 불변·스크롤바 0 ③ `/app.html` 대화 → 실 API 왕복 → 카드+근거 배지가 데스크톱·모바일 각 1회 관측 ④ smoke PASS 11/11·콘솔 red 0.

### 부수 관측
- **같은 실패를 두 파일에서 반복할 뻔했다**: 랜딩에서 미디어쿼리 소스 순서로 데인 직후 `app.html` 을 새로 짜면서 같은 구조를 만들었다. 그래서 app.html 의 모바일 블록은 처음부터 기본 규칙 **아래**에 두고 주석으로 이유를 박았다.
- 대화창 스크롤 정책이 두 화면에서 다르다 — 랜딩(대본 재생)은 스크롤 금지, app(세션 누적)은 허용. 둘 다 **상자 높이는 고정**이라 페이지가 튀지 않는 건 같다.

## DR9 — 진짜 Solar 대화 + 마커 카드 + 학식 정본화 (2026-07-21)

| # | 항목 | 결과 |
|---|---|---|
| 90 | `/api/chat` 신설 (step-1) | `solar-pro2` 구조화 출력으로 `{reply, search, budget, walkMax, tags}` 를 왕복 1회에 수신 |
| 91 | 데이터에 매인 응답 | "8천원 이하 혼밥"→budget 8000·tags[혼밥] / "여기 뭐 있어요?"→요약 후 카드 안내 / "파리 날씨"→"제가 가진 정보에는 없어요" |
| 92 | 인원 나눗셈 | "둘이서 2만원" → **1인 10,000원**, "만원으로 둘이" → 1인 5,000원 |
| 93 | 프롬프트 조정(1차 실측 후) | 응답이 식당 8곳을 나열 → "최대 2문장·80자·이름 2개"로 조임. tags 가 카테고리명(한식·중식)이라 매칭 불가 → 허용 어휘 명시 |
| 94 | **실패 프로브 ①** | `UPSTAGE_API_KEY` 없음 → 500 아닌 200 폴백(정규식으로 예산 8000 건짐). GET→405, 빈 메시지→400 |
| 95 | **실패 프로브 ②** | 타임아웃 1ms 강제 → 폴백 응답, 대화 안 끊김 |
| 96 | 대화창 연결 (step-2) | 배포본 네트워크: `/api/chat` 2회 · `parse-query` 0회 — 대화 경로가 실제로 산다 |
| 97 | **실패 프로브 ③** | `/api/chat` 강제 차단 → `parse-query` 폴백으로 조건 검색 계속, 카드 3장까지 진행, 버튼 복구 |
| 98 | 문구 교체 | 상단 = `한입지도` 한 줄 · 사이드 헤더 = `Upstage AI 에이전트` · 첫 인사 = `오늘은 뭘 먹어 볼까요?` · 데이터 카운터 제거 |
| 99 | **온도 실측** | temp 0/0.3/0.7 각 3회 — 조건 추출 3/3 로 동일하고 **온도 0 에서도 답변이 3종**. 다양성에 온도가 불필요해 구조화 안정성 우선(0.4)으로 정착 |
| 100 | **내부 태그 누출 적발** | 응답에 `<데이터>` 가 그대로 노출("날씨는 <데이터>에 없는 정보입니다") → 프롬프트 + 서버 후처리로 제거. 재측정 10회 누출 0 |
| 101 | 마커 카드 (step-3) | 클릭 → 아시안테이블 카드(메뉴 4행 + 외 5개 + 주소), 재클릭 → 0장, 다른 마커 → 1장 유지 |
| 102 | **카드 잘림 프로브** | 마커 중앙 정렬만으로는 위가 **70px 잘림**(카드가 마커 위에 서므로) → 렌더 후 실사각형을 재서 `panBy` 로 보정. 수정 후 상·하·좌·우 전부 지도 안쪽 |
| 103 | 학식 정본화 (step-4) | `패컬티 · 코너`(실재하지 않는 이름) 소멸. 4곳 정식 명칭 표시 — 출처 skku.edu welfare_11.do (접근 2026-07-21) |
| 104 | 당일 필터 | `/api/data` 가 KST 기준 `today` + `cafeteriaBoard` 제공. 2026-07-21 기준 패컬티식당 9,000원 운영 · 나머지 3곳 휴무 |
| 105 | **시트 stale 적발** | 처음엔 4곳 전부 휴무로 떴다 — 시트 마지막 적재가 7/16 이었다. 크롤러로 이번 주 5일치 적재 후 실물과 일치 |
| 106 | **실패 프로브 ④** | 데이터 0인 날 → 빈 화면이 아니라 4곳 전부 "휴무" 표시 |
| 107 | 하단 전체 목록 제거 | 24곳 나열 삭제 — 페이지 높이 데스크톱 4,170→2,000px대. 고아가 된 `emptyState()` 는 추천 빈 상태에 재연결 |
| 108 | **smoke 허수 적발** (step-5) | 목록 제거 후 smoke 가 9/11 FAIL 인데, **"예산 칩 필터" 항목은 검사 대상이 0개라 그냥 PASS** 였다 — 아무것도 안 재면서 통과를 찍는 허수. 검사 대상을 추천 카드로 옮기고 "검사할 가격이 실제로 있음" 가드를 추가 |
| 109 | smoke 고정 대기 결함 | 칩 클릭 후 2.5s 고정 대기로는 Solar 왕복(5~9s)을 못 기다려 또 0개가 됐다 → 조건 대기로 교체. 최종 **13/13 PASS(검사 15개)** |
| 110 | **결함 주입 재확인** | `#cafeteriaBlock` 을 깨뜨려 배포 → exit 1. 다만 예외로 죽어 어느 항목인지 안 남길래 FAIL 로 보고하도록 수정. 원복 후 13/13 exit 0 |
| 111 | 회귀 | 곁들임 규칙(DR6) 유지 — 1,000·2,000원 picks 0 / 3,000원 2건. 6페이지 전부 200 |
| 112 | 모바일 390px | 지도 300px·타일 47·마커 23 · 가로 넘침 0 · 학식 카드 4장 · 콘솔 red 0 |

**DoD 충족**: ① `/api/chat` 호출 관측·응답이 매번 다름 ② 마커 클릭 카드 열림/닫힘/전환 ③ 학식 정식 명칭 4곳·당일치·휴무 표시 ④ 하단 전체 목록 없음 ⑤ smoke 13/13·콘솔 red 0.

### 부수 관측
- **대화가 결정론적으로 보인 건 착시가 아니었다** — Solar 는 조건 추출에만 쓰이고 화면 문장은 전부 코드에 박혀 있었다. 사용자가 육안으로 짚어낼 때까지 검증 항목 어디에도 "이 문장을 누가 썼는가"가 없었다. 검증은 *동작*만 보고 *주장*은 안 봤다.
- 같은 실패 유형이 반복된다: **테스트가 사라진 셀렉터를 보며 통과**(DR8 `#picksBlock`, DR9 `#restaurantBlock`). 구조를 바꿀 때는 테스트가 "무엇을 몇 개 검사했는지"를 같이 출력해야 한다 — 그래서 검사 개수를 PASS 문구에 넣었다.

### DR9 검증 라운드 (2026-07-21) — 5회 반복

독립 검증을 5회 돌렸고 **4회 반증당했다.** 매 라운드가 실제 결함을 잡았다.

| 회차 | 판정 | 잡힌 것 |
|---|---|---|
| 1 | 반증 | 학식 수정이 app.html 에만 적용 — index/verify/test 는 `패컬티 · 코너`·8일 전 날짜 그대로 · 마커 카드 3px 잘림 · 예산 미추출 · 없는 가게에 딴소리 · smoke 허수 1건 |
| 2 | 반증 | 예산 추출이 **프롬프트에 넣은 예시 문장에만** 동작(다른 문형 6/6 실패) · 없는 가게 답변 5/6 실패 |
| 3 | 반증 | 숫자 없는 오타에 금액 환각 · `은(는)` 표기 노출 · 같은 사실 두 번 · `/api/parse-query` 미수정 |
| 4 | 반증 | 브랜드명 부분일치로 태그 오탐(`교촌치킨`→`치킨`) → "없습니다" 직후 추천 3곳 · 조건 없는데 문장은 금액을 읊음 · 십/백 단위·띄어쓴 숫자 미지원 |
| 5 | **확인** | BLOCKER·MAJOR 없음. MINOR 1건(아래) |

**남은 한계 (MINOR, 의도적 보류)**: 태그 오탐 방지가 모델의 `unknownPlace` 판정에 기대고 있어,
실재하지 않는 **음식 이름처럼 읽히는 조합**("동해생선카레")에서는 `생선` 태그가 살아 검색이 걸린다.
실제 브랜드명 6종(교촌치킨·김밥천국·네네치킨·장모초밥·미소야·명동칼국수)은 전부 정상 경로를 탔다.
데모에서 나올 법한 입력은 아니라 마감(7/23 미팅) 전 추가 손대지 않는다.

**이 반복에서 배운 것 — 검증 설계의 결함**
- 1~4회차 모두 "고친 표면만 고치고 나머지는 그대로"였다. 같은 판정 로직을 여러 화면이 각자
  구현하고 있으면 반드시 어긋난다 → **판정은 한 곳에 두고 데이터에 실어 보낸다**(곁들임 `isSide`,
  학식 `cafeteriaBoard`, 조건 `extractConditions`)가 이 프로젝트에서 세 번 반복된 교훈이다.
- **LLM 프롬프트로 결정론적 값을 뽑으려 한 것이 근본 설계 오류였다.** 지시를 더 붙이면 테스트한
  문장만 통과하고 다음 문형에서 샌다(2회차). 확정적으로 뽑히는 값은 코드가 뽑고, 모델은
  사람 말로 답하는 일만 맡는 구조로 바꾼 뒤에야 회귀가 멈췄다.
- 프롬프트 회귀는 단위 테스트가 못 잡는다 → `scripts/test-chat-extract.mjs` 29종으로 문형을 고정했다.

## DR10 — 품질 마감 (2026-07-21)

전수 QA 감사(독립) 선행 → BLOCKER 1·MAJOR 3·MINOR 4 확보 후 착수.

| # | 항목 | 결과 |
|---|---|---|
| 113 | **BLOCKER: 대화 필터 원문 복귀** (step-1) | `search=false` 턴 문장 필터가 **전부 걸러지면 원문으로 되돌아갔다** — 필터를 안 건 것과 같다. 실제로 "asdkjqwoie…" 입력에 "8,000원 이하 소반·홍순두부 추천" 이 화면의 다른 가게 카드와 함께 나갔다. 안전 문장으로 대체 |
| 114 | 조건 없는 입력 | 5종 × 2회 = **10/10 어긋남 0**(금액 주장·카드 안내 0건) |
| 115 | **필터 프로브** | 전 문장이 금액·카드로 채워진 응답을 넣어도 안전 문장으로 떨어지고, budget 있는 턴은 금액 문장 보존 |
| 116 | 학식 cron (step-2) | `vercel.json` crons 매일 21:10 UTC(06:10 KST). 시크릿 없이 → **401**, GET 외 메서드 → **405** |
| 117 | **cron 중복 사고와 원인** | 첫 실행이 같은 5행을 또 넣었다. 시트가 날짜를 `2026-07-12T15:00:00.000Z`(KST 자정의 UTC 표기)로 돌려주는데 크롤 값은 `2026-07-13` 이라 **멱등 키가 영원히 안 맞았다**. `normDate` 로 해소 → 재실행 `appended 0 / skipped 5` |
| 118 | 읽기 측 중복 제거 | 이미 들어간 중복은 화면에 남으므로 `loadSheetData` 에서도 걸러낸다 → 학식 14행 → **9행**, 오늘 카드 메뉴 12개(두 배 아님) |
| 119 | **크롤 실패 프로브** | 학교 페이지가 죽은 상황(잘못된 base 주입) → `status:"error"` · 시트 미변경 · 500 아님 |
| 120 | 크롤 로직 공유 | 서버·스크립트가 `api/_lib/cafeteria-crawl.js` 하나를 쓴다. 스크립트 124→**55줄** |
| 121 | 데모 마커 (step-3) | 회색 원 → SVG 핀. **마커 22개 = 시뮬 대상 22곳**(좌표 보유 23곳 중 1곳 제외) |
| 122 | 데모 입력 CTA | 대본 정지 → "여기는 소개용 데모" 안내 + `/app.html?q=…` 버튼. 클릭 시 실제 검색으로 카드 3장 + 근거 배지. 카드 높이 485 고정·스크롤바 0 |
| 123 | 모바일 nav (step-4) | 390px 에서 `Upstage AI` 13px 잘림 — **실제로는 스크롤됐지만 알 방법이 없어 고장으로 읽혔다**. 여백 축소 + 오른쪽 페이드, 끝에 닿으면 페이드 해제(`nav.js`) |
| 124 | nav 일관성 | 배포본 5페이지 전부 **5항목**(contribute.html 에 빠졌던 `Upstage AI` 추가) |
| 125 | `/test.html` | 배포 제외 → **404**. 파일은 원복 경로로 보존 |
| 126 | 헤드라인 간격 | `.amt` 고정폭 가운데 정렬 탓에 "8천원 으로" 로 띈 것처럼 보임 → 오른쪽 정렬. 금액 길이 3종에서 확인 |
| 127 | review 토큰 폼 | `<form>` 으로 감싸 콘솔 경고 해소 + 엔터 제출. click 핸들러를 submit 으로 이관 |
| 128 | 이슈 ⑦ (step-5) | 칵테일 4행 `비고='사이드'` → `isSide=true`. 사이드 83→87. **양방향 override**(`사이드`/`한끼`) 단위 4/4 |
| 129 | **이슈 ⑥ 진짜 원인** | 훅 문구는 이미 상류에서 정본 경로로 고쳐져 있었다(머신별 경로 2026-07-20 기각). 진짜 원인은 **이 레포가 `work.json` 을 git 추적**한 것 — 파일이 기기 사이를 오가며 서로를 덮어썼다. gitignore 로 이관, 이력은 ledger(3-event × 12)·ROADMAP(13 milestone)에 보존 확인 후 제거 |
| 130 | 완료 규칙 명문화 | `AGENTS.md`·`CLAUDE.md` 에 "ledger·ROADMAP·work.json 셋 다" 규칙 추가 |
| 131 | **smoke 허수 재확인** (step-6) | 학식 셀렉터 2개를 깨뜨려 배포 → **exit 1, 5항목 FAIL**. 원복 후 14/14 exit 0 |
| 132 | 전체 회귀 | 단위 29/29 + 41/41 · smoke **14/14** · 6페이지 200 / test.html 404 · 곁들임 1,000·2,000원 picks 0 · 콘솔 red 0 |

**DoD 충족**: ① 조건 없는 입력에 화면과 어긋나는 문장 0건 ② cron 실제 실행 관측·스케줄 등록 ③ 데모 카드 마커·입력 유도 ④ nav 동일·모바일 잘림 0·test.html 비공개 ⑤ 이슈 ⑥⑦ 닫힘 ⑥ 전부 PASS·콘솔 red 0.

### 부수 관측
- **자동화가 새 실패 유형을 데려왔다**: 사람이 돌릴 땐 안 보이던 멱등 결함(날짜 표기 불일치)이 cron 첫 실행에서 바로 터졌다. 자동화는 "돌아간다"가 아니라 **"두 번 돌려도 같은가"** 로 검증해야 한다.
- 독립 QA 감사를 먼저 돌린 게 이번 milestone 의 출발점이었다 — 내가 직전 라운드에 만든 BLOCKER 를 나 스스로는 못 찾았다.
