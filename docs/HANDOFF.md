# HANDOFF — 가져다 쓰려는 사람에게

> 2026-07-21 기준(§5 수치는 라이브 `/api/data` 와 대조 확인). 각자 버전을 만드는 팀원이 **자기 코드에 옮겨 쓸 수 있게** 쓴 문서다.
> "무엇을 만들었나"는 [README](../README.md)에, 여기엔 **어디서 막혔고 어떻게 풀었나**를 적는다.
> 이 레포를 통째로 가져갈 필요는 없다. 필요한 조각만 떼어 가면 된다.

---

## 0. 제일 먼저 알아야 할 것

이 프로젝트에서 진짜 어려웠던 건 **Upstage API를 호출하는 일이 아니었다.** 그건 반나절이면 된다.
어려웠던 건 그 다음이다 — **파싱 결과를 믿을 수 있게 만드는 일.**

파싱 정확도가 76.4%라는 건, 넷 중 하나는 틀린 채로 서비스에 나간다는 뜻이다.
그래서 이 레포의 절반은 파이프라인이고 나머지 절반은 **"틀린 걸 걸러내는 장치"**다.
심사 배점도 거기 몰려 있다(파이프라인 20 + 깊이 15 = 35점).

가져갈 게 하나뿐이라면 이걸 가져가라: **파싱 결과를 자동으로 저장하지 마라.**

---

## 1. 단계별로 막혔던 것과 해법

### 1-1. Document Parse — 좌표를 안 받으면 이름과 가격이 짝이 안 맞는다

기본 호출로 받으면 텍스트는 나오는데 **어느 가격이 어느 메뉴 것인지 알 수 없다.**
메뉴판은 왼쪽에 이름, 오른쪽에 가격이 떨어져 있는 2열 레이아웃이 많아서다.

```js
// api/parse-menu.js
ocr: "force",          // 이미지 안 텍스트를 강제로 다시 읽음
coordinates: true,     // 각 요소의 좌표 → 같은 줄끼리 묶을 수 있다
```

`coordinates: true`로 받아 y좌표가 비슷한 것끼리 묶는 로직이 `api/_lib/extract-menu.js`에 있다.

**표면별 정확도가 극단적으로 다르다** (`experiments/parse-poc/accuracy.md`):

| 사진 종류 | 정확도 |
|---|---|
| 인쇄 책자를 정면에서 | 100% |
| 유리창 세로 시트 | 100% |
| 타일 카드 (`3.0` = 3,000원 표기) | 66.7% |
| 손글씨 화이트보드 | 57.1% |
| 저해상도 웹 이미지 | 20.0% |

→ **사진을 어떻게 찍느냐가 모델보다 중요하다.** 수집 가이드에 "정면·밝게·가격 함께"를 넣어라.

후처리로 0% → 62.5% → 76.4%까지 올렸다. 실제로 효과가 컸던 것:
- OCR alt 텍스트 승격 (이미지로 인식된 영역에서 텍스트 건지기)
- 천원 단위 표기 복원 (`3.0` → 3,000)
- 공백 낀 숫자 (`4 000` → 4000)
- 한 줄에 여러 항목이 들어간 경우 분리

### 1-2. Solar — 모델 선택이 실측으로 갈렸다

`solar-mini`가 싸고 빠르지만 **"둘이서 2만원" 같은 나눗셈에서 예산을 틀렸다**(1인 1만원으로 못 나눔).
그래서 질의 구조화는 `solar-pro2`를 쓴다. 실측 왕복 0.7~1.3초.

```js
// 구조화 출력을 반드시 쓴다 — 자유 텍스트로 받으면 파싱이 또 문제가 된다
response_format: { type: "json_schema", json_schema: { name: "...", strict: true, schema: {...} } }
```

**타임아웃 폴백은 반드시 넣어라.** 데모 중에 외부 API가 느려지면 검색 자체가 죽는다.
`api/parse-query.js`는 실패 시 정규식 파서로 떨어지고, `api/recommend.js`는 이유 생성이 실패하면
데이터 기반 템플릿 문장으로 떨어진다. **추천 루프는 어떤 경우에도 죽지 않는다.**

### 1-3. 근거 검증 — 생성한 모델에게 채점을 시키면 안 된다

전용 Groundedness Check 모델은 2026-07 현재 계정 모델 목록에 없다(retired).
같은 계약을 **모델 분리**로 구현했다:

- 이유 생성 = `solar-pro2`
- 그 이유가 실데이터에만 근거하는지 판정 = `solar-mini` (독립 판정자)
- 판정이 `notGrounded`면 그 문장을 버리고 데이터 사실로 교체

화면의 "근거 검증됨" 배지가 이 판정 결과다. **환각을 막았다는 주장이 아니라 막는 동작을 보여준다.**
구현은 `api/recommend.js` 하단.

> 함정: 타임아웃이 짧으면 판정이 조용히 실패하고 배지가 안 뜬다. 실측 5%에서 그랬다.
> 생성 5s / 판정 3.5s로 올려 해결했다.

### 1-4. 검수 게이트 — 이게 없으면 파이프라인이 주장으로만 남는다

제보로 들어온 데이터는 시트에 `검수=대기`로 쌓이고, 운영진이 `/review.html`에서
**원본 사진과 나란히 놓고** 행마다 확인/제외/수정을 찍는다. `확인`이 찍힌 행만 서비스로 나간다.

```js
// api/_lib/sheet-data.js — 이 한 줄이 게이트다
.filter((m) => m.name && m.price && m.review === "확인")
```

**게이트는 우회 경로가 하나만 있어도 무너진다.** 우리는 실제로 그걸 겪었다:
- `api/data.js`에 Supabase 우선 경로가 있었는데 거기엔 게이트가 없었다. env 하나가 켜지면
  미검수 데이터가 그대로 나가는 구조 → 읽기 경로를 시트 하나로 단일화해 분기를 삭제했다.
- 화면 4개 중 3개만 고치고 완료로 볼 뻔했다(§3 참조).

### 1-5. 구글 시트를 DB로 쓰기 — 팀 협업에는 이게 낫다

DB를 쓰면 팀원이 데이터를 못 본다. 시트는 **누구나 열어서 눈으로 확인하고 고칠 수 있다.**
검수라는 행위 자체가 사람이 하는 일이라 시트가 잘 맞았다.

- **읽기**: 공개 gviz CSV — 키 없이 서버에서 읽힌다. `api/_lib/sheet-data.js`
- **쓰기**: Apps Script 웹훅 — 권한이 필요해 서버 함수에서만. `api/_lib/sheet-write.js`, `scripts/apps-script/`

> 함정 ①: 시트 기록은 **항상 `USER_ENTERED`**로. `RAW`로 쓰면 gviz가 헤더를 흡수해버린다.
> 함정 ②: Apps Script cold start가 10초를 넘긴 적이 있다. 타임아웃 20초로 잡았다.
> 함정 ③: **행 삭제 기능을 안 만들었다.** 그래서 테스트 제보를 넣으면 영구히 남는다. 조심.

---

## 2. 그대로 떼어 갈 수 있는 조각

빌드 도구 없음, 프레임워크 없음. 파일 단위로 복사된다 — 단 **같이 가져가야 하는 파일과
반드시 고쳐야 하는 하드코딩이 있다.** 아래는 코드를 실제로 훑어서 확인한 목록이다.

### 이식 난이도 순

**① `api/_lib/side-menu.js` — 그냥 복사된다**
`isSideMenu(menu)` 순수 함수. import 없음, env 없음, I/O 없음.
가져갈 가치는 함수가 아니라 **구조**다: 규칙 기반 판정 + 시트 `비고` 열로 개별 오버라이드(`사이드`/`한끼`).
안에 든 정규식(한국 주류 브랜드, `^공기밥`, `세트|정식`, `100g` 단가 표기)은 도메인 어휘라 그대로 쓸 거면 그대로, 아니면 전부 갈아야 한다.

**② `api/parse-menu.js` + `api/_lib/extract-menu.js` — 키만 있으면 된다**
- 같이 가져갈 파일: 이 둘 (`parse-menu.js` → `_lib/extract-menu.js` 하나만 import)
- env: `UPSTAGE_API_KEY`
- 런타임: Vercel handler `(req, res)`, POST 전용, raw body를 `for await (const c of req)` 로 읽는다 → **Express/Next 라우트로 옮기면 이 부분을 그 프레임워크 방식으로 바꿔야 한다.**
- 고칠 곳: `extract-menu.js` 의 가격 정규식이 **원화·한글 전제**다(`원`, `3.0`→3000 복원, `[가-힣]` 트리밍). 한국어 메뉴판이 아니면 오파싱한다.

**③ `api/parse-query.js` — 프롬프트를 자기 도메인으로 갈아야 한다**
- 같이 가져갈 파일: **`api/chat.js`** (`extractConditions`, `parseAmount` 를 여기서 import 한다 — 이 파일만 떼어 가면 import 에러로 바로 깨진다)
- env: `UPSTAGE_API_KEY`, `SOLAR_TIMEOUT_MS`(기본 2500)
- 고칠 곳: `SYSTEM_PROMPT` 전체가 "성균관대 명륜캠 주변 식당" 전제다. 태그 어휘(`혼밥,단체,회식,격식,해장,야식,24시,가성비,양많음,데이트,시험기간`)와 `80m=1분` 도보 환산도 여기 박혀 있다.
- **함정**: 정규식 폴백에도 태그 목록이 따로 들어 있다. 프롬프트만 고치고 폴백을 안 고치면 **평소엔 맞다가 Solar 타임아웃 때만 옛 태그가 나온다.**

**④ `api/_lib/sheet-data.js` — 시트 스키마가 코드에 박혀 있다**
- 같이 가져갈 파일: `api/_lib/side-menu.js`
- env: 없음. 공개 링크 시트를 gviz CSV로 읽어서 **키가 필요 없다** (이게 이 파일의 장점)
- 고칠 곳: `SPREADSHEET_ID`(우리 팀 시트), 탭 이름 `식당`·`메뉴`·`학식`, 그리고 **컬럼이 위치 기반**이다(`r[0]`=id, `r[1]`=이름, `r[3]`=주소, `r[4]`/`r[5]`=위경도…). 열 순서가 다르면 `.map()` 을 다시 써야 한다.
- 게이트 판정이 문자열 `review === "확인"` 이다. 시트의 승인 표기가 다르면 여기.
- 캐시는 모듈 레벨 `Map`(60초) — **서버리스에서는 warm 인스턴스에서만 산다.** 캐시 히트를 전제로 설계하지 마라.

**⑤ `scripts/geocode-sheet.mjs` — 좌표를 반드시 갈아라**
- env: `KAKAO_REST_API_KEY` (없으면 exit 2)
- **외부 의존: `gws` CLI** (구글 시트 읽기/쓰기를 shell out 한다). 이 레포에 없는 도구이고 별도 구글 인증이 필요하다. 이게 없으면 안 돌아간다.
- 고칠 곳: `CAMPUS = { lat: 37.5877, lng: 126.9938, maxKm: 2.5 }` — 명륜캠 정문 좌표다. 키워드 검색 오탐을 반경으로 걸러내는 용도라, **안 고치면 다른 지역 가게가 조용히 전부 탈락한다.** 쓰기 범위 `식당!D:F` 도 열 배치 전제.

**⑥ `scripts/crawl-cafeteria.mjs` — 크롤링 로직은 이식 대상이 아니다**
- 같이 가져갈 파일: `scripts/_lib/gws.mjs`, `api/_lib/cafeteria-crawl.js`, `api/_lib/cafeterias.js` (전이 의존)
- **외부 의존: `gws` CLI** (위와 동일)
- 대상 URL(`skku.edu/…/welfare_11.do`)·캠퍼스 코드(`conspaceCd`)·HTML 파싱 정규식이 전부 성대 그 페이지 전용이다. **다른 학교면 크롤링부터 다시 짜는 게 맞다.**
- 가져갈 만한 건 크롤러가 아니라 **중복 제거 후 append 하는 CLI 패턴** 쪽이다.

> 학식 크롤러는 **팝업/JS 없이 direct GET으로 됐다**. Playwright 안 써도 된다 — 이건 확인했다.

> **공통 함정**: `SPREADSHEET_ID` 가 ④⑤⑥ 세 곳에 각각 하드코딩돼 있다. 시트를 바꾸면 **세 곳 다** 고쳐야 한다.
> (우리도 이걸 상수 하나로 못 모았다. 옮길 거면 그때 모아라.)

---

## 3. 우리가 실제로 저지른 실수 — 같은 데 빠지지 마라

**① 화면 하나만 고치고 다 고쳤다고 믿었다.**
곁들임(공기밥 1,000원)이 한 끼처럼 추천되던 버그를 `api/recommend.js`에서 고치고 완료로 봤다.
독립 검증을 돌렸더니 **홈 화면·검증 페이지·구 테스트 페이지가 각자 자기 필터를 갖고 있어서**
같은 예산에 서로 다른 답을 내고 있었다. 두 번의 검증에서 두 번 반증당한 뒤에야 닫혔다.
→ **판정 로직은 한 곳에만 두고 데이터에 플래그로 실어 보내라.** 화면마다 규칙을 다시 쓰면 반드시 어긋난다.

**② 배포 파일 목록을 손으로 관리했다.**
Vercel 배포는 "목록에 적힌 파일이 전부"다. 새로 만든 3개 페이지가 목록에 없어서,
그대로 한 번 더 배포했으면 **라이브에서 그 페이지들이 삭제될 상태**였다.
→ 목록을 손으로 들지 말고 디렉터리를 재귀 수집해라(`scripts/deploy-staging.mjs`).

**③ 유닛 테스트 통과를 "동작한다"로 착각했다.**
API 응답만 확인하면 통과처럼 보이는데, 실제 브라우저를 열면 목록과 지도가 안 고쳐져 있었다.
→ **배포본을 실브라우저로 열어봐라.** 모바일 폭(390px)도 반드시. 우리 헤더는 거기서 무너져 있었다.

**④ 발표 자료가 코드보다 먼저 낡는다.**
7/17에 쓴 덱이 7/20에 이미 틀렸다(URL·수치·없어진 계획). 더 나쁜 건 **그 사이에 만든 제일 좋은 기능
두 개가 덱에 아예 없었다는 것.** → 기능 하나 끝낼 때마다 덱을 한 줄이라도 고쳐라.

---

## 4. 수집 단계에서 미리 해둬야 하는 것 — 우리가 뒤늦게 깨달은 것

**데이터를 넣을 때 "이 값을 어디서 봤는지"를 같은 행에 남겨라.** 나중에 추가할 수 없다.

우리는 안 남겼다. 그래서 이런 일이 있었다 — `R015 소반` · `R016 홍순두부` ·
`R017 작은마을생선구이` · `R018 프라이팬` · `R019 그 남자의 파스타 대학로` 5곳이
검수 대기로 쌓였는데, **[식당] 탭에 메뉴판 사진이나 출처 링크 열이 없어서 대조할 원본이 없었다.**
"사람이 사진과 대조해 확인한다"가 이 서비스의 핵심 주장인데, 하필 그 근거가 비어 있는 행이 생긴 것이다.
(결국 팀이 다른 경로로 확인해 닫았다 — 노출 식당 14 → 24곳. `docs/OPEN-ISSUES.md` ① )

검수 게이트를 만들 거라면 **게이트에 필요한 증거를 수집 스키마에 처음부터 넣어라**:

| 열 | 왜 |
|---|---|
| 원본 사진 링크 (또는 파일명) | 검수자가 대조할 대상. 이게 없으면 검수가 "믿고 넘어가기"가 된다 |
| 출처 URL | 사진이 아니라 웹에서 주웠을 때의 대조 대상 |
| 수집자 · 촬영일 | 값이 낡았는지 판단. 우리는 이 둘은 넣어놨다(`collector`, `shot_date`) |

나중에 채우려면 **그 데이터를 누가 어디서 가져왔는지 기억하는 사람을 찾아야 한다.** 대개 못 찾는다.

---

## 5. 데이터 현황 (2026-07-20)

구글 시트 3개 탭. 식당 24곳 · 학식 4일치. [메뉴] 탭은 이렇게 갈린다:

| 구분 | 행 수 |
|---|---|
| 데이터 행 총계 | 374 |
| └ 검수 `확인` | 343 |
| └ 검수 `제외` | 30 (대부분 `TEST` id 의 E2E 잔여 행 26개) |
| └ 검수 `대기` | 1 (예시행 `R000`) |
| **실제 서비스로 나가는 행** | **342** (`확인` 343 − 예시행 R000 1) |

`342` 는 `GET /api/data` 가 돌려주는 수와 같다 — 게이트가 실제로 작동하는지 이 숫자로 대조하면 된다.
`검수` 열은 F열(0-based 인덱스 5)이다.

> 이 표는 처음에 348/343/4/1 로 잘못 적었다. `R###` 형식의 id 만 세느라 `TEST` id 로 들어간
> E2E 잔여 26행을 놓쳤다. 독립 검증에서 잡혔다 — **시트를 셀 때 id 형식으로 거르지 마라.**

사진 원본과 파싱 캐시는 `experiments/parse-poc/`에 있다 — 어떤 사진이 어떤 결과를 냈는지 대조 가능.

**지금 열려 있는 이슈는 [docs/OPEN-ISSUES.md](OPEN-ISSUES.md)에 있다.**
