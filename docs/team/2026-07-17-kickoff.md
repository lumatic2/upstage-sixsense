# 팀 킥오프 계약 + 모듈 분배 (테크리드: 전유성)

> 2026-07-17 작성 · **2026-07-17 개정 v2** (개인화 축 M-H/M-I 추가, M-F 시드 데이터 원정으로 확장 — 사용자·테크리드 합의)
> 팀: 김민서 · 이준범 · 정경화 · 전유성 (4인) · 마감: 빌드업 7/24(금) · 데모데이 7/25(토)
> 원칙 출처: 분배 단위 = 역할이 아니라 **인터페이스로 격리된 독립 작업** (지난 팀전 실측 — 역할 분담은 리더 몰빵으로 끝났다)

## 0. 팀 계약 (첫 미팅에서 합의할 것 — 미확정 항목은 ☐)

- 테크리드 = **전유성**: 완성물 구조 파악 · 독립 조각 분배 · 인터페이스 조율 · PR 머지(조립 게이트). *직접 개발도 하지만 병목이 되지 않는 게 우선.*
- ☐ 결정권: 의견 갈릴 때 최종 결정자 지정 (제안: 제품 방향 = 팀 다수결, 기술 = 테크리드, 스키마 = 이준범)
- ☐ 발표자 지정 + 발표 준비는 **발표 1시간 전 착수 금지** — 7/23까지 PPT 완성, 7/24 리허설.
- 작업 가시화: **hanipmap 레포 GitHub 이슈**로만 분배 (이슈로 안 쪼개지면 아직 분배 단위가 아님). 팀원 전원 write 권한 초대.
- 결정 기록: 결정은 이슈/PR 스레드에 남긴다 — 카톡에만 있는 결정은 없는 결정.
- 완료 판정: 조각 완료 = 본인 선언이 아니라 **인수 기준(각 모듈 Verify) 통과 + 테크리드 확인**.

## 1. 최종 아웃풋 (조립 완성 상태)

hanipmap.vercel.app 하나에서, 사용자 경험 순서로:

1. **처음 온 사람**: 예산을 고르면 → 학식 + 주변 식당이 지도에 표시 + 하단 카드 리스트. 자연어 검색("과선배가 사는 날 2만원 둘이")도 같은 필터에 꽂힘(Solar 구조화).
2. **식당 선택**: 상세 정보(메뉴·가격·위치) + 네이버 길찾기 링크.
3. **다시 온 사람**: 켜자마자 과거 선택 기록 기반 개인화 추천이 첫 화면에 뜸 (예산 조절·직접 선택 여전히 가능). 추천 이유는 Solar가 문장으로 생성.
4. **데이터 공급**: 메뉴판 **사진 1장**으로 제보(Document Parse 프리필→수정→pending) — 지도에 뜨는 메뉴·가격은 이 파이프라인 + 팀 시드 원정(M-F) + 학식 크롤러가 채운다.
5. **발표**: 루브릭 6항목 대응 PPT + 위 시나리오 시연.

**전 조각의 동작 레퍼런스**: https://upstage-sixsense-staging.vercel.app/test.html (이 레포 `public/test.html` — 코드가 곧 스펙)

### 개인화 추천 설계 (M-I의 스펙, 2026-07-17 리서치 확정)

- **계정**: Supabase `signInAnonymously()` — 로그인 화면 없이 앱 켜는 순간 계정 생성, 추후 `linkIdentity()`로 소셜 승격 가능 (https://supabase.com/docs/guides/auth/auth-anonymous, 접근 2026-07-17).
- **기록**: `user_events` 테이블 (user_id, place_id, action: 노출/선택/상세/길찾기, ts) + RLS 본인 한정.
- **추천 점수** = w₁·예산적합도 + w₂·태그 선호도(과거 선택 태그 유사도) + w₃·frecency(빈도+최근성 지수감쇠, Firefox 랭킹 검증 패턴) + w₄·인기도. 신규 유저는 기록이 없어 w₂·w₃ 항이 자연히 0 → cold start 분기 로직 불필요.
- **오픈소스 추천 엔진(Gorse 등)은 비채택** — 상시 서버 필요(serverless 부적합) + 이 규모에선 collaborative filtering이 통계적으로 무의미. PPT "확장 계획"에서 이벤트 스키마가 Gorse 호환임을 언급(lake를 크게 잡은 흔적).
- 구현은 Postgres 함수 또는 serverless JS — 별도 인프라 0.

## 2. 모듈 분배 (독립 조각 × 고정 인터페이스)

> 표의 "인터페이스"는 **동결**이다 — 바꾸고 싶으면 구현 전에 테크리드에게. 각 조각은 서로를 기다리지 않는다.

| # | 모듈 | 담당 | 고정 인터페이스 (입력→출력) | 레퍼런스 | 인수 기준 (Verify) |
|---|---|---|---|---|---|
| M-A | **사진 제보 UI** — hanipmap 제보 폼에 파싱 연결 | 김민서 | `POST /api/parse-menu` `{image: base64, filename}` → `{items:[{name,price}]}` (에러: `{error,code}`) | test.html §① 코드 | 실사진 업로드→프리필→항목 수정→기존 pending insert 경로로 제출. 실패 사진(비이미지)에서 에러 메시지 표시 |
| M-B | **검색·예산 UI** — 검색창을 parse-query 로 교체 + 첫 화면 예산 슬라이더 | 정경화 | `POST /api/parse-query` `{query}` → `{budget, walkMax, tags, reason, source}` (항상 200 — 폴백 내장). 예산 슬라이더는 같은 필터 함수 직접 호출 | test.html §② 코드 | 질의 3종 + 슬라이더 조작에서 기존 필터·지도에 값 전달. `source` 를 화면 어딘가에 표시(데모용) |
| M-C | **서버·DB 기반** — api/ 이식 + 스키마 적용 + 집계 | 이준범 | 이 레포 `api/*.js` 복사(무수정) + `db/schema.sql` 적용(M-I 테이블 포함) + Vercel env `UPSTAGE_API_KEY` | `docs/research/2026-07-17-m3-e2e.md` §이식 체크리스트 | hanipmap 배포에서 두 API 200 + `cafeteria_menus`·`user_events` 테이블 존재 + before/after SQL 숫자(trd-requests ②) 회신 |
| M-D | **학식 표시** — 추천 풀에 학식 카드 노출 | 김민서 or 정경화 (M-A/B 먼저 끝낸 사람) | `cafeteria_menus` SELECT (읽기 공개) 또는 임시로 `/cafeteria-sample.json` | test.html §③ | 오늘 날짜 학식이 지도/리스트에 보임 |
| M-E | **발표 PPT + 리허설** | 전원 (초안 조립: 정경화 제안) | `docs/presentation/deck.md`(원고)·`rubric-map.md`·`qna.md` → 팀 PPT. 개인화·확장계획 슬라이드 추가 | deck.html 렌더본 | 루브릭 6항목 슬라이드 존재 + 7/24 전체 리허설 1회 |
| M-F | **시드 데이터 원정** (비개발) — 전원 각 5~8곳 명륜동 실촬영→파싱→검수→DB | 전원 | 실촬영 사진 → 사진 제보 파이프라인(우리 것 dogfood) → `experiments/parse-poc/accuracy.md` 검수 행 추가 | `experiments/parse-poc/README.md` | 식당 ≥20곳 시드 입력 + 재측정 표본 ≥3장 — 발표의 "실측 정확도"·"직접 수집 1차 데이터" 서사 강화 |
| M-G | **조립·운영** — PR 리뷰, 크롤러 스케줄, 스테이징 유지, 인터페이스 분쟁 조정 | 전유성 | — | — | 7/23 통합 E2E(사진 제보→승인→추천 노출→개인화 반영) 1회 통과 |
| M-H | **상세 화면 + 길찾기 + 개인화 홈** | ☐ (M-A/B 진행 보고 배정) | 카드 탭→상세(메뉴·가격·위치) + 네이버 길찾기 링크. 첫 화면: `GET /api/recommend` → `[{place, score, reason}]` 렌더. M-I 완성 전엔 목 JSON으로 개발 | test.html + 목 recommend 응답(테크리드 제공) | 카드→상세→길찾기 동선 동작 + 재방문 시 추천 카드가 첫 화면에 뜸 |
| M-I | **계정·기록·추천 백엔드** — 익명 auth + events API + 스코어링 | 전유성 (스키마 협의: 이준범) | `POST /api/events` `{place_id, action}` / `GET /api/recommend` → `[{place, score, reason}]` (reason 은 Solar 생성) | §1 개인화 설계 | 이벤트 3종 기록→recommend 점수에 반영되는 것을 SQL 로 증명 + 신규 유저(기록 0)도 200 응답 |

- **events 호출 심기**(쉬움)는 M-I 소유가 아니라 각 UI 조각 담당이 자기 화면에서 한 줄씩 — M-A(제보 제출 시), M-B(검색 시), M-H(선택·상세·길찾기 시). 리더 몰빵 방지선.

## 3. 바이브코딩 착수 프롬프트 (각자 AI에 이 문단 + 레퍼런스 파일을 주고 시작)

- **M-A (김민서)**: "hanipmap 레포의 제보 폼에 사진 업로드를 추가하고 싶어. 첨부한 test.html 의 §① 로직(파일→base64→POST /api/parse-menu→응답 items 를 폼에 프리필)을 우리 제보 폼 스타일에 맞게 이식해줘. 자동 저장은 하지 말고, 사용자가 수정한 뒤 기존 제출 버튼으로 pending 제출되게."
- **M-B (정경화)**: "hanipmap 의 검색 입력이 지금 정규식 파싱을 쓰는데, 첨부한 test.html §② 처럼 POST /api/parse-query 를 호출해서 {budget, walkMax, tags} 를 받아 기존 필터 함수에 넘기게 바꿔줘. 응답의 source 필드를 검색 결과 옆에 작게 표시해줘. 그리고 첫 화면에 예산 슬라이더를 추가해서 같은 필터 함수를 직접 호출하게 해줘."
- **M-C (이준범)**: `docs/research/2026-07-17-m3-e2e.md` 하단 "hanipmap 이식 시 남는 것" 4항목이 그대로 작업 목록. api 파일은 수정 없이 복사가 원칙(다르게 하고 싶으면 먼저 얘기).
- **M-H (담당 확정 후)**: "hanipmap 에서 식당 카드를 탭하면 상세 화면(메뉴·가격·위치)이 뜨고 네이버 지도 길찾기 링크가 있게 해줘. 그리고 첨부한 목 JSON 형태의 GET /api/recommend 응답을 첫 화면 추천 카드로 렌더해줘 — 실제 API 는 나중에 URL 만 바꿔 끼울 거야."
- 막히면: 스크린샷+에러를 이슈에 붙이고 다음 조각으로 — **혼자 3시간 이상 파지 않기.**

## 4. 조립 일정 (역산)

| 날짜 | 마일스톤 |
|---|---|
| 7/18(금) | 팀 미팅: 이 문서 합의 + GitHub 초대 + 이슈 생성(M-A~I) + M-C 권한·env 완료 + **M-F 원정 구역 배분** |
| 7/20(일) | M-A·M-B 첫 동작(스테이징 API 로 개발 가능 — M-C 안 기다려도 됨). M-H 목 데이터로 착수. M-I 스키마 확정 |
| 7/22(화) | M-C 완료 → 전 조각을 hanipmap 배포로 전환. M-I recommend 실 API 전환. M-F 시드 ≥20곳 입력 완료 |
| 7/23(수) | **통합 E2E**(제보→승인→추천→개인화) + M-F 재측정 마감 → 발표 숫자 동결 |
| 7/24(목) | PPT 완성 + 전체 리허설 (빌드업 마감) |
| 7/25(토) | 데모데이 |

## 5. 리스크·인터페이스 분쟁 규칙

- M-A/M-B/M-H 는 **스테이징 URL·목 데이터로 즉시 개발 시작 가능** — M-C(권한)·M-I 지연이 전체를 막지 않는다 (지난 팀전의 "리더 흡수" 재발 방지선).
- 인터페이스를 바꾸고 싶어지면 = 그 조각이 잘못 쪼개진 신호일 수 있음 → 구현 전에 테크리드와 재분해.
- api 코드 수정 금지 원칙(M-C): 검증된 코드를 그대로 옮기는 것이 초심자 팀의 최선 — 개선 아이디어는 이슈로.
- **M-I 축소선**(7/22 까지 스코어링 미완 시): recommend 를 "예산적합+인기도"만으로 출시하고 frecency·태그 선호는 데모 전날 추가 — 개인화 서사는 이벤트 기록 화면만으로도 성립.
