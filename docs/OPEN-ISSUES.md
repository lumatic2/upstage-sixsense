# OPEN ISSUES — 열린 이슈 / 사용자 결정 대기

> 2026-07-20 DR5 완료 시점 기준. 데모데이 7/25 · 빌드업 마감 7/24.
> 닫힌 이슈는 여기서 지우고 `verification/matrix.md` 또는 ADR 로 옮긴다.

## ① 미검수 식당 5곳 · 메뉴 74행이 서비스에서 빠져 있음 — **결정 대기**

[메뉴] 탭에서 `검수` 열이 **빈 값**인 행 74개. 게이트(`검수="확인"` 만 통과)를 못 넘어 서비스에 안 나온다.
현재 노출 식당 14곳 → 풀면 **19곳**. 데모 밀도에 직접 영향.

| ID | 식당 | 메뉴 행 | 수집자 | 메뉴판 사진 |
|---|---|---|---|---|
| R015 | 소반 | 3 | 김민서(웹수집) | 없음 |
| R016 | 홍순두부 | 2 | 김민서(웹수집) | 없음 |
| R017 | 작은마을생선구이 | 22 | 김민서(웹수집) | 없음 |
| R018 | 프라이팬 | 15 | (공란) | 없음 |
| R019 | 그 남자의 파스타 대학로 | 32 | (공란) | 없음 |

**막힌 지점**: [식당] 탭에 메뉴판 사진 링크가 없다. 대조할 원본이 없으면 "사람이 사진과 대조해 확인한다"는
파이프라인 주장이 거짓이 되므로 **임의 승인 금지**.

- (A) 팀원에게 그 5곳의 **원 출처(웹 URL 등)** 를 받아 `비고` 열에 남기고 대조 후 검수 — **추천**
- (B) 검수 없이 노출 — 반대. DR4 에서 세운 게이트를 스스로 깨는 것
- (C) 그대로 둠 — 데모에 5곳 빠진 채로 진행

## ② 보류 4건 판정 — 바로 착수 가능

[메뉴] `검수=대기` 행 (R000 예시행 제외):

- `R003 성대한김밥 / 공기밥 / 1000` — 사이드. ③과 함께 처리
- `R004 아시안테이블 / 타이 포크 미트볼 / 5000`
- `R007 해피니스 대학로점 / 고기 듬뿍 간짜장 / 9000`
- `R013 페르시안궁전 / "인기준" / 12500` — **파싱 쓰레기값으로 보임, 제외 후보**

검수는 `/review.html` (운영진 전용, `hanip.reviewToken` sessionStorage 필요).

## ③ 사이드 메뉴가 한 끼처럼 추천됨 — 바로 착수 가능

`공기밥 1,000원`(R003) · `꼬치 1개 1,000원`(백미향마라탕) 등이 저예산 검색에서 한 끼처럼 나온다.
DR5 실측: 예산 1,000원 검색 시 이것들만 나옴.
최소 수정안: [메뉴] `비고` 열에 `사이드` 표시 → `api/recommend.js` 에서 **단독 pick 제외**(목록에는 유지).

## ④ `api/data.js` Supabase 경로에 검수 게이트 없음 — **결정 대기**

Supabase env 가 있으면 그쪽을 우선하는데 그 경로엔 게이트가 없다. 현재 env 미설정이라 inert.

※ 2026-07-20 정정: "필터를 추가하면 된다"는 **틀렸다** — Supabase `menus` 테이블에 `review` 열 자체가 없다.
또 `scripts/sync-sheet-to-db.mjs` 의 upsert 는 삭제를 안 해서, DR4 이전에 올라간 미검수 행이 있으면 영구 잔존한다.

- (A) `api/data.js` 의 Supabase 분기 삭제 (시트가 SSOT) — **추천**
- (B) `review` 열 추가 + 동기화·읽기 양쪽 수정 — Supabase 를 계속 쓸 때만
- 전역 규칙상 죽은 코드는 임의 삭제 금지 → 사용자 답변 필요

## ⑤ `KAKAO_REST_API_KEY` 없음 — secret 필요

제보 좌표 자동 채움이 안 돼 새 제보 식당이 지도에 안 찍힌다.
Vercel env 에 키를 넣으면 그 뒤는 코드 수정 없이 동작한다.

## ⑥ 하네스 장부 이원화 — 이 레포 밖 문제

`.harness/work.json`(SKILL.md 가 지정한 **정본**) vs `.harness/machines/<host>/work.json`(훅 메시지가 지시).
그래서 DR4 는 머신 로컬에만, DR5 등록은 정본에만 들어갔다. 지금은 양쪽에 DR5 완료를 넣어 맞췄으나
**DR4 가 정본에 없는 상태는 그대로**다.
고칠 자리는 `~/projects/custom-skills` 의 `/harness` 원본(훅 문구를 정본 경로로) — 배포본 직접 편집 금지.

## 작업 시 주의 (DR4·DR5 에서 실측으로 얻은 것)

- 시트 **행 삭제 금지**. `검수` 열 값 변경만 (Apps Script 에 delete action 자체가 없다).
- 사진 대조 없이 `확인` 찍기 금지 — 파이프라인의 유일한 진실성 담보.
- `public/test.html` 삭제 금지 (`/verify.html` 승격 실패 시 원복 경로).
- 신규 디자인 토큰 발명 금지 — `public/theme.css` 기존 변수만.
- `scrollIntoView({behavior:"auto"})` 금지 — CSS `scroll-behavior:smooth` 를 물려받아 씹힌다. `"instant"` 로 고정.
- 서버 코드 검증 시 정적 서버만으로는 `/api` 가 죽어 "데이터 0"인지 "필터 0"인지 구분이 안 된다 —
  `/api/*` 는 배포본 프록시 + 수정 중인 핸들러만 직접 import 하는 dev 프록시가 필요하다.
- 이 레포의 git `origin` 은 GitHub 가 아니라 **m4 로컬 경로**다. GitHub 는
  `https://github.com/lumatic2/upstage-sixsense.git` — macair 에서는 임시 remote 를 붙였다 뗐다 했다.
