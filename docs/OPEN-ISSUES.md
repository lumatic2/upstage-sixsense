# OPEN ISSUES — 열린 이슈 / 사용자 결정 대기

> 2026-07-21 DR10 시점 기준. 닫힌 이슈는 여기서 지우고 `verification/matrix.md` 또는 ADR 로 옮긴다.

## 열려 있는 것

| 번호 | 이슈 (2026-07-22 신규) |
|---|---|
| ⑩ | **"휴무" 가 "크롤 실패" 를 가린다** — 시트의 오늘자 학식 행이 0이면 `todayBoard` 는 원인과 무관하게 4곳을 `open:false` 로 내려주고, 화면은 랜딩 카운터 "휴무" · app 카드 "휴무" 로 똑같이 그린다. **정상 휴무(주말·방학)와 파이프라인 정지가 사용자에게도 우리에게도 구분되지 않는다.** 실제로 2026-07-21 에 적재가 5일 밀려 "4곳 전부 휴무" 가 떴다(`api/cron/crawl-cafeteria.js:4`). 후보 해법: 보드에 마지막 적재 날짜를 실어 보내 오늘과 다르면 "휴무" 대신 "정보 없음"으로 말한다 (신선도는 이미 시트에 있다 — 새 수집이 필요 없다). |

## 2026-07-22 닫힌 것 (DR11)

| 이슈 | 결말 |
|---|---|
| 라이브 `about.html` 이 ADR-0004 로 폐기된 순서 개인화를 광고 | **닫힘.** 삭제가 아니라 재서술 — 축을 닫은 판단 자체가 정직성의 증거다. 검증 중 같은 문장이 로그인 다이얼로그를 통해 **4페이지에 복제**된 것을 추가 적발 |
| `docs/SITEMAP.md` 가 구현과 6곳 어긋남 + 자기 모순 | **닫힘.** 7건 코드 1:1 대조로 불일치 0. 재발 방지로 문구·라벨 규약(§5)과 문서 동기화 규약(`CLAUDE.md`·`AGENTS.md`) 신설 |
| 랜딩 카운터 "학식" 이 전 날짜 누적 행 수 | **닫힘.** `cafeteriaBoard` 기반 학식당 개수로 교체. cron 이 돌수록 불어나던 숫자였다 |
| 지도 마커 카드 잘림 (사용자 실관측) | **닫힘 — 진단이 뒤바뀌어 있었다.** 랜딩이 아니라 `/app.html` 이었고, 보정이 있는데도 `panBy` 가 애니메이션이라 1초쯤 잘려 보였다. 미는 대신 그리기 전에 자리를 고른다. 마커 14개 전수 잘림 0 |
| `contribute` 0행 폴백·정확도 표기 미구현 | **닫힘.** 수기 입력 폴백 + 76.4% 명시 |
| cron 첫 자동 실행 미관측 | **닫힘.** 2026-07-22 `today` 에 당일 식단 존재 — 사람 손 없이 갱신 확인 |

## 2026-07-21 닫힌 것

| 구 번호 | 이슈 | 결말 |
|---|---|---|
| ⑨ | `CAT_BONUS=60` 의 세기 | **닫힘 — 값이 아니라 축이 문제였다.** 카드 클릭은 눌러도 아무 일도 안 일어나는 반응 없는 클릭이었고, 우리는 일어나지 않은 선택으로 순서를 바꾸고 있었다. 순서 개인화를 통째로 걷어내고 개인화를 "대화 연속성"으로 재정의했다 → `docs/adr/0004-personalization.md` |
| ⑧ | 대화 태그 판정이 모델의 `unknownPlace` 에 기대고 있음 | **닫힘.** 태그를 **어절 앞머리**에서만 잡도록 바꿨다(앞 글자가 한글이면 낱말 속에 묻힌 것으로 보고 버린다). "동해생선카레"→태그 0, "생선구이"·"치킨 먹고싶어"는 그대로. 띄어쓰기를 안 한 정상 입력을 놓치는 건 남긴다 — 조건을 못 잡는 편이 지어내는 것보다 낫다. `changesets/20260721-tag-word-boundary/` |
| ⑥ | 하네스 장부 이원화 | **닫힘.** 훅 문구는 이미 상류에서 정본 경로로 고쳐져 있었다(머신별 경로는 2026-07-20 기각). 남은 건 이 레포 쪽 정합이었다 — `.harness/work.json` 을 git 추적에서 빼고(`checkout-local` 규약) 잔여 `machines/` 를 제거했다. **파일이 git 으로 기기 사이를 오간 것이 DR4 갈라짐의 진짜 원인이었다.** 완료 처리 규칙(ledger·ROADMAP·work.json 셋)을 `AGENTS.md`·`CLAUDE.md` 에 명문화 |
| ⑦ | 곁들임 규칙이 못 잡는 칵테일 | **닫힘.** 시트 [메뉴] 164~167행 `비고='사이드'` 표기 → `isSide=true` 확인. 사람이 규칙을 이기는 경로가 실제로 도는 것을 양방향(`사이드`/`한끼`)으로 검증 |

## 2026-07-20 닫힌 것

| 구 번호 | 이슈 | 결말 |
|---|---|---|
| ① | 미검수 식당 5곳·메뉴 74행 | 팀이 검수 완료 — 노출 식당 14 → 24곳 |
| ② | 보류 4건 판정 | 확인 3 · 제외 1(`R013 인기준` = OCR 잔여물) |
| ③ | 사이드 메뉴가 한 끼처럼 추천됨 | `api/_lib/side-menu.js` 판정 + `isSide` 부착 → 5개 노출면 전부 적용 |
| ④ | `api/data.js` Supabase 게이트 없음 | 분기 삭제, 시트 SSOT 단일화 |
| ⑤ | `KAKAO_REST_API_KEY` 없음 | Vercel env 등록 |
| — | 본선 조립본(hanipmap 이식) | 폐기 — `docs/adr/0003-no-assembly-build.md` |

## 작업 시 주의 (DR4~DR10 실측)

- 시트 **행 삭제 금지**. 열 값 변경만 (Apps Script 에 delete action 이 없다).
- 사진 대조 없이 `확인` 찍기 금지 — 파이프라인의 유일한 진실성 담보.
- **시트에 테스트 제보를 남기지 마라** — 지울 수 없어 영구 잔존한다. 배선 확인은 `/api/config` 의 `ready` 플래그로.
- **시트는 날짜를 `2026-07-12T15:00:00.000Z`(KST 자정의 UTC 표기)로 돌려준다.** 크롤 값(`2026-07-13`)과
  그대로 비교하면 멱등 키가 영원히 안 맞아 매일 중복이 쌓인다 — `normDate` 를 거쳐라.
- **판정 로직을 화면마다 새로 쓰지 마라.** 곁들임·학식 명칭·조건 추출에서 세 번 같은 사고가 났다.
  판정은 한 곳(`api/_lib/`)에 두고 데이터에 플래그로 실어 보낸다.
- **LLM 프롬프트로 결정론적 값을 뽑지 마라.** 지시를 더 붙이면 테스트한 문장만 통과하고 다음
  문형에서 샌다(실측 6/6 실패). 확정적으로 뽑히는 값은 코드가, 모델은 사람 말로 답하는 일만.
- `public/test.html` 삭제 금지(`/verify.html` 승격 실패 시 원복 경로) — 단 **배포에서는 제외**한다.
- 신규 디자인 토큰 발명 금지 — `public/theme.css` 기존 변수만.
- `scrollIntoView({behavior:"auto"})` 금지 — CSS `scroll-behavior:smooth` 를 물려받아 씹힌다. `"instant"` 로 고정.
- **모바일 미디어쿼리는 기본 규칙 아래에 둔다** — 위에 두면 같은 명시도에서 밀려 무시된다(지도가 0px 이 됐다).
- **배포는 "적힌 파일이 전부"다** — `deploy-staging.mjs` 는 자동 수집이니 하드코딩 목록으로 되돌리지 마라.
- 로컬에 시트 쓰기 자격이 없다 — `node scripts/with-vercel-env.mjs <스크립트>` 로 배포 env 를 빌린다(`VERCEL_TOKEN` 필요).
- GitHub: `https://github.com/lumatic2/upstage-sixsense.git`
