# Plan — M2 메뉴판 파싱 PoC (차별화 축 검증)

> Milestone: `ROADMAP.md` M2 · Horizon: `harness:goal id="product-horizon"` (2026-07-25 데모데이)
> 근거: ADR-0001 · `experiments/parse-poc/README.md` (판정 기준 정본)
> 작성: 2026-07-17 · CS3 dogfood horizon 일괄 계획 1/3

Status: approved
- Execution mode: continuous
- Stop only on: blocked · decision_required · risk_gate · secret_required
- Rollback/cleanup: 실험 산출물은 `experiments/parse-poc/` 격리 — 실패 시 파일 삭제로 원복, 코드·DB 부작용 없음. API 비용은 사진 1장당 $0.01(크레딧 $10 내).

## 스캐폴딩 결정
- 작업 유형: analysis
- source-of-truth: `experiments/parse-poc/accuracy.md` — 정확도 실측·판정의 정본
- deploy: 해당 없음 — 로컬 스크립트 실험(배포면 없음)
- 테스트 표면: `node scripts/parse-menu-poc.mjs <사진들>` 실행 + 출력 JSON을 사진 원본과 눈 대조(골든셋)

## 범위와 중단점
- 이번 milestone 이 닫는 것: "메뉴판 사진 속 가격을 Document Parse 로 해방할 수 있다" 가설의 실측 검증 (정확도 수치 + 판정).
- 범위 밖: api/parse-menu.js 본구현(M3), 실촬영 사진 재측정(사진 도착 시 골든셋 추가 — M2 재오픈 아님, accuracy.md append).
- 중단점: UPSTAGE_API_KEY 인증 실패(blocked) · 웹에서 판독 가능한 메뉴판 이미지를 3장 미만밖에 못 구함(decision_required — 실촬영 대기로 전환 여부).

## Step 트리

- [ ] step-1 명륜동 메뉴판 사진 수집 (웹 — 혼합 1단계)
  - Artifact: analysis — `experiments/parse-poc/photos/` 이미지 3~5장 + `photos/sources.md`(출처 URL·접근일·식당명)
  - Files: `experiments/parse-poc/photos/*` (신규 write)
  - Dependencies: 없음
  - Verify: 판독 가능한 메뉴판(메뉴명+가격 보이는) 이미지 ≥3장 저장 + sources.md 에 전 장 출처 기록. 화질 다양성(정면·비스듬·저화질 혼합) 확인.
  - Failure probe: 음식 사진만 있고 메뉴판이 없는 식당은 sources.md 에 "skip: 메뉴판 없음" 으로 기록 — 무증거 수집 금지.
  - Commit: `chore(M2): PoC 골든셋 사진 수집` 1커밋
- [ ] step-2 Document Parse 실행 + 정확도 측정 + 판정
  - Artifact: analysis — `experiments/parse-poc/*.raw.json`·`*.menu.json` + `accuracy.md`(표 + 종합 정확도 + 판정)
  - Files: `experiments/parse-poc/accuracy.md`, `experiments/parse-poc/*.json` (신규 write) · `scripts/parse-menu-poc.mjs` (read)
  - Dependencies: step-1 사진 · `.env` 의 UPSTAGE_API_KEY
  - Verify: 사진별 행(실제 항목 수/정확/이름틀림/가격틀림/누락/비고) 전부 채워진 accuracy.md + 종합 정확도 산출 + README 기준 판정(≥90 본구현 / 70~90 수정전제 진행 / <70 옵션 실험 후 멘토링 질문행) 명시.
  - Failure probe: 일부러 잘못된 키로 1회 호출해 HTTP 401 에러 경로(스크립트가 죽지 않고 continue) 확인 · API 실패 사진은 원인(status·응답)을 accuracy.md 비고에 기록.
  - Commit: `feat(M2): 파싱 PoC 실측 — 정확도 N% 판정` 1커밋
- [ ] step-3 M2 완료 처리
  - Artifact: changeset — ledger 3-event + ROADMAP M2 완료
  - Files: `.harness/execution-ledger.jsonl`, `ROADMAP.md` (write)
  - Dependencies: step-2 판정
  - Verify: `roadmap_sync.py status` 에서 M2 completed + Evidence 경로 = accuracy.md
  - Failure probe: 판정이 <70% 이면 M2 를 완료가 아니라 `blocked`(재실험·멘토링 대기) 로 두는지 확인 — 실패 데이터도 산출물이지만 DoD(정확도 측정)는 충족되므로 이 경우도 completed + 판정 기록으로 닫되, M3 step-1 진입 전 decision_required 로 정지.
  - Commit: ROADMAP·ledger 갱신 커밋

## 결정 로그
- status: resolved
- UPSTAGE_API_KEY 확보: 사용자 제공 키를 `.env` 로 보관 (2026-07-17 확정) — 커밋·로그 노출 금지.
- 사진 확보: 혼합 — 웹(네이버/카카오 업체 사진) 수집으로 즉시 실행, 실촬영 도착 시 골든셋 append (2026-07-17 확정).
- 판정 기준: `experiments/parse-poc/README.md` 의 3구간(≥90/70~90/<70)을 그대로 사용 — 새 결정 없음.
